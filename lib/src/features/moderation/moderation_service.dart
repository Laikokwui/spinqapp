class ModerationService {
  ModerationService();

  /// Quick keyword-based block list check (local)
  bool isBlocked(String text) {
    // TODO: load local blocked phrases and heuristics; return true if blocked
    return false;
  }
}
