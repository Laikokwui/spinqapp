# Copilot / Assistant Instructions

- Preferred state management: `flutter_riverpod`.
- Include unit tests for logic and providers when implementing features.
- Follow folder structure under `lib/src/` and keep features isolated.
- Do not commit API keys, credentials, or model files. Use `ENV.example`.
- Run `flutter analyze` and `flutter test` before proposing changes.
